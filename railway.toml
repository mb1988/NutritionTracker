[build]
builder = "NIXPACKS"
buildCommand = "npx prisma generate && npm run build"

[deploy]
preDeployCommand = ["node tasks/railway-migrate.js"]
startCommand = "npm start -- -p $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
