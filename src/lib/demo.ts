/** Well-known ID for the shared demo user row in the DB. */
export const DEMO_USER_ID = "demo-user";

/** Email stored on the demo user record (not a real address). */
export const DEMO_USER_EMAIL = "demo@nutrition-tracker.app";

/** Cookie name that, when set to "1", activates demo mode. */
export const DEMO_COOKIE = "demo_mode";

